{
  "name": "owldex",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "clean": "lerna run clean && lerna clean",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.11.1"
  }
}
