import React from "react";
import { Dialog } from "../../../components/dialog";

const Deck = () => (
  <div>
    <Dialog label="You don't have cards" text="Add some fun to your deck" />
  </div>
);

export default Deck;
